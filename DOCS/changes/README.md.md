File: README.md
Change: Updated README to document v5.0.0 behavior: main-image-only default, reconcile endpoint, and added notes about logout behavior in the popup.
Details: Mentioned that `sync_config.json` holds server token and that local session is stored in extension storage; added brief usage notes.
Timestamp: 2026-08-14T09:15:00+03:00
