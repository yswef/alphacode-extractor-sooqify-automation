# خطة إعادة هيكلة الباك اند (Backend Refactor Plan)
### مشروع: alphacode-extractor-sooqify

> **الهدف العام:** تفكيك `app.py` (اللي فيه حالياً ~90% من منطق المشروع) إلى هيكلة طبقات نظيفة (Layered / Clean Architecture)، تتبع مبادئ **SOLID**، بحيث يقدر أي مطور (أو أي موديل ذكاء اصطناعي يكمل بعدك) يفهم المشروع ويضيف مميزات بسهولة بدون ما يكسر شي.

---

## ⚠️ قواعد إلزامية لأي موديل/جلسة تشتغل على هذا المشروع

هذي القواعد تنطبق على **كل مرحلة** أدناه، بدون استثناء:

1. **لا نقل ملفات كاملة في الرد** — فقط الملفات أو الأجزاء المعدَّلة، مع ذكر المسار الدقيق.
2. **ممنوع تخمين سبب أي خلل أو تعديل منطق مشكوك فيه.** إذا الموديل مو متأكد 100%:
   - يتوقف عن التعديل.
   - يكتب دالة/سكربت اختبار (Test Report) للجزء المشكوك فيه.
   - يطلب من المستخدم يشغّله ويرجع بالـ Logs.
   - ينتظر النتائج قبل أي تعديل فعلي.
3. **كل تعديل جوهري (منطق برمجي / دالة رئيسية) لازم يترافق فوراً مع تحديث ملف `.md`** يوثّق: أي شي تغيّر، ليش تغيّر، وكيف يشتغل المنطق الجديد.
4. **بعد كل قسم من المهام (Phase)**، لازم يُكتب تقرير في `PROGRESS.md` (الملف موضح بالأسفل) قبل الانتقال للمرحلة التالية.
5. **ما ينزل بأي مرحلة تالية إلا بعد ما تنتهي المرحلة الحالية وتنفحص (تشغيل المشروع + اختبار يدوي).**

---

## 🗂️ ملف التتبع (يُنشأ أول شي — Phase 0)

أنشئ ملف `backend/PROGRESS.md` بهذا الشكل، ويُحدَّث بعد كل مرحلة:

```markdown
# Progress Tracker

| Phase | الحالة | تاريخ الإنجاز | ملاحظات |
|-------|--------|----------------|----------|
| 0 - Inventory & Analysis | ⏳ لم يبدأ | - | - |
| 1 - Setup Structure | ⏳ لم يبدأ | - | - |
| 2 - Config & State Layer | ⏳ لم يبدأ | - | - |
| 3 - Extract Services | ⏳ لم يبدأ | - | - |
| 4 - Extract Repositories | ⏳ لم يبدأ | - | - |
| 5 - API/Routes Layer | ⏳ لم يبدأ | - | - |
| 6 - Watchers/Background Jobs | ⏳ لم يبدأ | - | - |
| 7 - Tests & Cleanup | ⏳ لم يبدأ | - | - |
```

> **لأي موديل جديد يدخل المشروع:** اقرأ هذا الملف أولاً لتعرف وين وقف الموديل اللي قبلك، واقرأ آخر تقرير في `docs/reports/` المرتبط بالمرحلة.

---

## 🎯 الهيكلة المستهدفة النهائية

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                    # نقطة الدخول / app factory فقط
│   ├── api/                        # طبقة العرض (Controllers/Routes)
│   │   ├── __init__.py
│   │   └── routes/
│   │       ├── upload_routes.py
│   │       ├── reports_routes.py
│   │       └── sync_routes.py
│   ├── core/                       # إعدادات ونقاط ثابتة
│   │   ├── config.py               # تحميل .json configs
│   │   └── constants.py
│   ├── services/                   # منطق الأعمال (Business Logic) - SRP
│   │   ├── report_service.py
│   │   ├── sync_service.py
│   │   ├── variant_extractor_service.py
│   │   └── upload_service.py
│   ├── repositories/                # طبقة الوصول للبيانات (JSON/DB)
│   │   ├── paths_repository.py
│   │   ├── sync_state_repository.py
│   │   └── sync_queue_repository.py
│   ├── models/                      # الكيانات / DTOs / Schemas
│   │   └── ...
│   ├── watchers/                    # المهام الخلفية (watch_variant_extractor)
│   │   └── variant_watcher.py
│   └── utils/                       # دوال مساعدة عامة فقط
├── config/                          # ملفات json الإعدادات (تبقى هنا)
│   ├── paths_config.json
│   ├── reports_config.json
│   └── sync_config.json
├── data/                            # ملفات الحالة المتغيرة (runtime state)
│   ├── sync_queue.json
│   └── sync_state.json
├── tests/
├── logs/
├── docs/
│   └── reports/                     # تقارير كل مرحلة (md)
├── PROGRESS.md
├── requirements.txt
└── run.py                            # سكربت التشغيل فقط
```

**مبادئ SOLID المطبّقة هنا تحديداً:**
- **S (Single Responsibility):** كل service يسوي شغلة وحدة (extract, sync, report, upload) بدل ما تكون كلها جوه `app.py`.
- **O (Open/Closed):** الـ routes تعتمد على واجهات الـ services، فإضافة ميزة = إضافة service/route جديد بدون تعديل القديم.
- **L (Liskov):** أي repository بديل (مثلاً تبديل JSON بقاعدة بيانات لاحقاً) يجب يشتغل بنفس الواجهة بدون ما يكسر الـ services.
- **I (Interface Segregation):** كل repository/service له واجهة صغيرة مركزة على مسؤوليته فقط.
- **D (Dependency Inversion):** الـ routes والـ services تعتمد على "عقود" (interfaces/abstract classes) مو على تفاصيل التنفيذ المباشرة.

---

## 📋 المراحل (Phases) بالتفصيل

### **Phase 0 — الجرد والتحليل (Inventory & Analysis) — بدون أي تعديل على الكود**

> هدف هذي المرحلة فقط الفهم والتوثيق، **ممنوع تعديل أي سطر كود فيها**.

المهام:
1. افتح `app.py` و`Reports.py` و`watch_variant_extractor.py` واقرأها بالكامل.
2. اكتب في `docs/reports/00_inventory.md` جدول يحتوي:
   - اسم كل Function/Class الموجودة حالياً.
   - المسؤولية الحالية لها (سطر أو سطرين).
   - أي ملفات config/state تستخدمها.
   - هل فيها منطق متشابك (Coupling) مع دوال ثانية.
3. حدد أي دوال "مشكوكة" (منطقها غير واضح أو فيها Side Effects غريبة) → اكتبها في قسم منفصل باسم "نقاط تحتاج توضيح من المستخدم" بدل ما يتم تخمين وظيفتها.
4. لا تنتقل لأي Phase تالية قبل ما يراجع المستخدم هذا الملف ويجاوب على النقاط المشكوكة.

**مخرجات المرحلة:** `docs/reports/00_inventory.md` فقط. لا كود جديد.

---

### **Phase 1 — إنشاء الهيكل الفارغ (Scaffolding)**

المهام:
1. أنشئ كل المجلدات المذكورة في "الهيكلة المستهدفة" فوق، مع ملفات `__init__.py` فاضية.
2. انقل ملفات الـ config الحالية (`paths_config.json`, `reports_config.json`, `sync_config.json`) إلى `backend/config/` **بدون تعديل محتواها**.
3. انقل ملفات الحالة (`sync_queue.json`, `sync_state.json`) إلى `backend/data/`.
4. حدّث أي مسار (path) في `app.py` يشير لهذي الملفات ليطابق الموقع الجديد فقط (تعديل مسارات، مو منطق).
5. تأكد إن المشروع لسا يشتغل بنفس الطريقة القديمة بعد نقل الملفات فقط (اختبار تشغيل يدوي من المستخدم).

**مخرجات المرحلة:** الهيكل الفارغ + `docs/reports/01_scaffolding.md` يوضح كل ملف انتقل من وين لوين.

---

### **Phase 2 — طبقة الإعدادات والحالة (Config & State Layer)**

المهام:
1. أنشئ `app/core/config.py`: دالة/كلاس واحد مسؤول فقط عن تحميل وقراءة ملفات الـ config (بدل ما تكون قراءة JSON متكررة بأماكن مختلفة في `app.py`).
2. أنشئ `app/repositories/sync_state_repository.py` و`sync_queue_repository.py`: كل وحدة تتكفل بالقراءة/الكتابة لملفها فقط (get/save/update).
3. عدّل `app.py` بحيث يستخدم هذي الطبقات الجديدة **بدل القراءة المباشرة من JSON**، بدون تغيير أي منطق عمل فعلي — فقط تغيير "كيف" يوصل للبيانات.
4. أي جزء فيه شك بالمنطق الأصلي → طبّق القاعدة رقم 2 (توقف واطلب اختبار).

**مخرجات المرحلة:** الملفات الجديدة + `docs/reports/02_config_state_layer.md`.

---

### **Phase 3 — استخراج طبقة الـ Services (المنطق الأساسي)**

المهام (كل وحدة تُنجز وتُختبر لحالها قبل الانتقال للي بعدها):
1. `variant_extractor_service.py`: انقل منطق استخراج المتغيرات (Variants) من `app.py`/`watch_variant_extractor.py` بدون تعديل السلوك.
2. `report_service.py`: انقل منطق `Reports.py` بحيث يصير Service مستقل تستدعيه أي route.
3. `sync_service.py`: انقل منطق المزامنة (sync) الموجود حالياً متوزع بالملف الرئيسي.
4. `upload_service.py`: انقل منطق الرفع (استناداً على `test_upload_main_image_only.py` كمرجع لفهم السلوك المتوقع).

**قاعدة صارمة هنا:** النقل يعني **Copy-paste مع تنظيم**، مو إعادة كتابة المنطق من الصفر. أي تغيير بالمنطق نفسه (مو بس الموقع) يحتاج تأكيد صريح من المستخدم أولاً.

**مخرجات المرحلة:** 4 ملفات Services + `docs/reports/03_services_extraction.md` (يوضح كل دالة انتقلت من وين لوين، وأي تعديل طفيف صار مثل تغيير أسماء).

---

### **Phase 4 — طبقة الـ Repositories المتبقية**

المهام:
1. أنشئ `paths_repository.py` للتعامل مع `paths_config.json`.
2. تأكد إن كل الـ Services (من Phase 3) تستخدم الـ Repositories بدل الوصول المباشر لأي ملف JSON.
3. لا تسمح بوجود أي `open()` مباشر لملفات JSON خارج طبقة الـ repositories.

**مخرجات المرحلة:** `docs/reports/04_repositories.md`.

---

### **Phase 5 — طبقة الـ API/Routes**

المهام:
1. أنشئ `api/routes/upload_routes.py`, `reports_routes.py`, `sync_routes.py`.
2. كل route يستدعي الـ Service المناسب فقط (route = واجهة استقبال، مو منطق).
3. `app/main.py` يصير بس مسؤول عن تجميع الـ routes وتشغيل التطبيق (App Factory Pattern).
4. `app.py` القديم يصير فاضي أو يُحذف بعد التأكد إن كل شي انتقل ونجح الاختبار.

**مخرجات المرحلة:** `docs/reports/05_api_layer.md`.

---

### **Phase 6 — الـ Watchers / المهام الخلفية**

المهام:
1. انقل `watch_variant_extractor.py` إلى `app/watchers/variant_watcher.py`.
2. تأكد إنه يستخدم `variant_extractor_service.py` بدل تكرار المنطق.

**مخرجات المرحلة:** `docs/reports/06_watchers.md`.

---

### **Phase 7 — اختبارات ونظافة نهائية**

المهام:
1. راجع `test_upload_main_image_only.py` وانقله لمجلد `tests/` مع تحديث الـ imports.
2. أضف اختبارات بسيطة (Smoke Tests) لكل Service تتأكد إنه يرجع نفس النتائج القديمة.
3. حدّث `CHANGELOG.md` بملخص كامل للهجرة.
4. تأكد `requirements.txt` نظيف ومحدث.

**مخرجات المرحلة:** `docs/reports/07_final_cleanup.md` + تحديث `CHANGELOG.md`.

---

## 📝 قالب التقرير المطلوب بعد كل مرحلة

يُحفظ في `docs/reports/0X_اسم_المرحلة.md`:

```markdown
# تقرير المرحلة: [اسم المرحلة]
**التاريخ:** ...
**الحالة:** مكتملة / جزئية / متوقفة بانتظار اختبار

## الملفات المتأثرة
- مسار الملف → نوع التغيير (إنشاء/نقل/تعديل)

## ملخص التغيير
...

## سبب التغيير
...

## كيف يعمل المنطق الجديد
...

## نقاط تحتاج اختبار يدوي من المستخدم
- [ ] ...

## نقاط مشكوك فيها لم تُعدَّل (بانتظار توضيح)
- ...
```

---

## 🔄 كيف تنتقل بين حسابات/موديلات مختلفة بدون فوضى

1. قبل ما تخلص الجلسة الحالية (تجربتك المجانية)، تأكد إن آخر تقرير محفوظ فعلاً في `docs/reports/`.
2. حدّث `PROGRESS.md` بحالة آخر مرحلة (مكتملة/جزئية).
3. بالحساب/الموديل الجديد، أول رسالة تكتبها له:
   > "اقرأ `backend/PROGRESS.md` وآخر ملف بـ `docs/reports/`، وكمّل من حيث توقف الموديل السابق، بنفس القواعد المذكورة بالأعلى."
4. هذا يخلي أي موديل (سواء نفس الجلسة أو جلسة/حساب جديد) يكمل بدون ما يعيد اكتشاف المشروع من الصفر.
