# توثيق تسليم شامل — مشروع AlphaCode Extractor

**الغرض من هذا الملف:** ملخص كامل لكل ما اكتُشف وأُصلح وبقي مفتوحاً خلال جلسة عمل طويلة على هذا المشروع، موجّه لموديل ذكاء اصطناعي (كلود أو غيره) يبدأ من الصفر بمراجعة الكود. اقرأ هذا **قبل** أي فحص للكود — يوفر عليك ساعات من إعادة اكتشاف نفس المشاكل.

**بنية المشروع:** نظامان منفصلان يعملان معاً:
1. **Backend** — خادم Flask (Python) محلي، يدير الأرشيف/المزامنة/معالجة الصور.
2. **Extension** — إكستنشن متصفح Chrome (JavaScript)، يستخرج بيانات المنتجات من مواقع الموردين ويرفعها لمنصة Sooqify عبر الباك اند.

---

## القسم 1: منهجية العمل المُثبتة بهالمشروع (مهم تتبعها)

بعد تجربة طويلة، هذي القواعد أثبتت فعاليتها ولازم أي موديل جديد يلتزم فيها:

1. **لا تفترض من اسم الملف/الدالة أنها مستخدمة فعلاً.** أخطر نمط تكرر عدة مرات هنا: كود "منقول" ظاهرياً لمكان جديد، لكن القديم يبقى موجوداً بالتوازي (Shadowing) — النسخة القديمة تكون هي الشغّالة فعلياً بينما الجديدة معزولة/غير موصولة. اكتشفناها 3 مرات منفصلة بهالمشروع (تفصيل بالقسم 2).
2. **تحقق بالتنفيذ الفعلي، مو بقراءة الكود فقط.** MD5 hash، تشغيل ديناميكي، pytest، تشغيل السيرفر فعلياً — كلها كشفت مشاكل ما كانت تبين من مجرد قراءة الكود.
3. **لا تخمّن سبب أي خطأ.** اطلب سجلات (`alphacode.log`)، اطلب لقطات شاشة فعلية، اطلب محتوى الملف المُنزَّل. التخمين أدى لإصلاحات ناقصة أكثر من مرة (مثال: إصلاح `ProductType` الأول كان بمكان غلط بالـ payload، ما كان له أي تأثير).
4. **القرارات الموثقة الخاصة بهذا المشروع تفوز على أي نص عام بخطة/قالب.** مثال: قرار "watchers/ فاضي" فاز على نص خطة عام يقول "انقل لملف watcher".
5. **لا تعتبر أي مرحلة/إصلاح مكتمل إلا بعد اختبار فعلي ناجح.** أكثر من تقرير بهذا المشروع أعلن "اكتمال" وتبين لاحقاً إنه ناقص جوهرياً.

---

## القسم 2: حالة الباك اند (Flask)

### تاريخ الريفاكتور
كان ملف واحد ضخم `backend/app.py` (~3750 سطر)، جاري تقسيمه لطبقات (Clean Architecture) عبر خطة موثقة بـ `Backend refactor plan.md` وتقارير `docs/reports/00` إلى `07`.

### القرارات التسعة الموثقة (`docs/reports/00_inventory.md`)
هذي قرارات **متعمدة** اتخذها المستخدم أو الفريق قبل أي عمل ريفاكتور — لا تخمّن حلولاً بديلة لها، ولا تعيد فتحها بدون سؤال المستخدم صراحة:
1. `/api/dry-run` يستدعي `load_settings`/`parse_sizes_list` **غير معرّفتين إطلاقاً** — الـ endpoint معطوب بالكامل، تُرك عمداً "لا يُنقل حتى التحقق اليدوي".
2. `reports_config.json` — بقايا قديمة، خارج نطاق الهجرة الحالية.
3. AI cache (`ai_copy_cache.json`) — كود ميت، مستبعد عمداً من الهجرة.
4. `sync_background_worker()` (حلقة دورية قديمة) — **كود ميت غير مستدعى من أي مكان**. لا يُنقل كسلوك حي لطبقة الـ service. تُرك متعمداً بـ`app.py` (أو مكانه الحالي بعد Phase 5) لحين تنظيف لاحق.
5. صيغة سعر الساعة المعتمدة: `(OriginalPrice_yuan + WatchFlatFeeYuan) × ExchangeRate` — دالة `build_watch_variations_from_absolute_yuan` بـ`upload_service.py` تطبّقها، لكنها **لم تُستدعَ من أي route لحد آخر تحقق** (الـ route عنده حسابه inline منفصل بدون استخدام هذي الدالة).
6. `watch_variant_extractor.py` القديم → أصبح shim إعادة تصدير من `variant_extractor_service.py`.
7. `Reports.py` القديم → نفس الشيء، shim لـ`report_service.py`.
8. **قرار مهم:** `variant_extractor_service.py` هو المصدر الوحيد الصحيح لمنطق استخراج متغيرات الساعات. **مجلد `app/watchers/` يجب أن يبقى فاضياً أو يُحذف — ممنوع إنشاء `variant_watcher.py`** حتى لو نص خطة عام يقول عكس ذلك.
9. `require_root_dir` — تفصيل غير موثق بالكامل هنا، راجع `00_inventory.md` الأصلي لو احتجته.

### مسار الأرشيف متغيّر وقت التشغيل — نقطة حرجة
`ARCHIVE_PATH` (وملفات ثانية مثله) **ليست ثوابت** — تُعاد حسابها وقت التشغيل حسب مجلد الحفظ اللي يختاره المستخدم من الإعدادات (`/api/paths/choose-folder`، دالة `recompute_paths()`). أي repository/module جديد يتعامل مع هذا المسار **يجب يستقبله كمعامل صريح**، لا يحسبه أو يخزّنه بنفسه وقت الاستيراد — وإلا ينكسر لأي مستخدم غيّر مجلد الحفظ.

### حالة كل مرحلة (لحد آخر تحقق فعلي مني)

| المرحلة | الحالة الموثقة | ملاحظة حرجة |
|---|---|---|
| 0 (جرد) | ✅ | — |
| 1 (نقل config/state لهيكل فارغ) | ✅ | — |
| 2 (استخراج repositories أولية: paths/sync_queue/sync_state) | ✅ | — |
| 3 (استخراج services: report/sync/variant_extractor/upload) | ✅ (بعد إصلاح) | **كانت الملفات "منقولة" بس الأجسام القديمة بقيت مكررة بـ`app.py` (Shadowing) — اكتُشف وأُصلح بالتحقق الفعلي (تحميل ديناميكي + `__module__`)** |
| 4 (استخراج repositories متبقية: archive/sync_config) | ✅ (تحقق فعلي) | تم بنفسي مباشرة، مع الانتباه لمشكلة المسار المتغيّر أعلاه |
| 5 (طبقة الـ Routes) | ⚠️ **حالة غير مؤكدة — آخر ما أعرفه: كانت غير مكتملة جوهرياً** | فصّل بالأسفل |
| 6 (Watchers) | ✅ (مبني على قرار 8: حذف بدل إنشاء) | — |
| 7 (اختبارات ونظافة) | ⚠️ **نُفّذت فوق Phase 5 غير المؤكدة** | راجع الأسفل |

### 🔴 المشكلة الأخطر المعروفة: Phase 5 غير مكتملة (آخر حالة مؤكدة)

بالتحقق الفعلي (MD5 hash + مقارنة قوائم `@app.route`)، تبيّن:
- `backend/app.py` كان **مطابقاً حرفياً (نفس MD5)** لنسخة نهاية Phase 4 — **صفر تغيير**، رغم إن Phase 5 كانت موسومة ✅.
- بالتوازي، تم بناء نظام routes جديد فعلي وشغّال (`backend/app/main.py` + `backend/app/api/routes/{upload,reports,sync}_routes.py`، ~1568 سطر) — **لكن غير متصل بأي شيء**. السيرفر الفعلي اللي كان يشتغل (`python app.py`) يستخدم النسخة القديمة بالكامل.
- 5 مسارات كانت **غائبة كلياً** عن النظام الجديد: `/api/brands`, `/api/brands/add`, `/api/health`, `/api/paths/choose-folder`, `/api/paths/status`.
- تقرير Phase 6 نفسه (`06_watchers.md`، قسم Next Steps) قال صراحة إن "هدم `app.py` النهائي" لسا مهمة **مستقبلية**. تقرير Phase 7 تجاهل هذا وأعلن اكتمالاً شاملاً بدون تنفيذه فعلياً.

**آخر إجراء:** أرسلت تصحيح مفصّل لموديل آخر (جيميناي) كان يعمل على الكود مباشرة، بالخطوات: (1) نقل الـ5 مسارات الناقصة، (2) تدقيق الـ33 route الموجودة فعلاً تستدعي services صح ومو نسخ منطق، (3) **تشغيل فعلي لـ`app/main.py` والتأكد من نجاح endpoints حرجة قبل حذف أي شيء**، (4) حذف الـ38 `@app.route` من `app.py` القديم بعد التأكد، (5) تحديث نقطة التشغيل الفعلية، (6) تصحيح سردية تقارير 05/06/07 و`CHANGELOG.md`.

**لم يصلني تأكيد نهائي بنجاح هذا التصحيح.** أول شي تسويه: اطلب `backend/app.py` الحالي + `backend/app/api/routes/*.py` + `docs/reports/05_api_layer.md`، وكرر نفس التحقق (MD5 مقارنة بنسخة Phase 4 المذكورة هنا، ومقارنة قوائم المسارات) قبل ما تصدّق أي تقرير "مكتمل ✅".

---

## القسم 3: حالة الإكستنشن (Chrome Extension)

**لم يخضع لأي ريفاكتور منظم بعد** — كل الإصلاحات كانت نقاط محددة (bug fixes) وسط تشخيص. جرد شامل منظم **لم يكتمل** (بدأته وتوقفت بطلب المستخدم لصالح هذا التوثيق).

### حجم الملفات
| الملف | الأسطر | الدور |
|---|---|---|
| `content.js` | ~4089 | المنطق الرئيسي: استخراج من صفحة المورد، معاينة، رفع فردي/دفعة |
| `admin_autofill.js` | ~2983 | تعبئة نموذج Sooqify تلقائياً بعد الرفع |
| `background.js` | ~1497 | Service worker: طابور الدفعة، تخزين، توسيط رسائل بين الأجزاء |
| `popup.js` | ~1447 | واجهة popup الإكستنشن (**لم يُفحص إطلاقاً بعد**) |
| `content.css` | ~2398 | تنسيقات محقونة بصفحة المورد |
| `page_bridge.js` | ~462 | جسر قراءة بيانات React + اعتراض شبكة بصفحة المورد |
| `config.js` | ~120 | `DEFAULT_CONFIG` — القيم الافتراضية المشتركة |
| `popup.html` | ~487 | هيكل واجهة popup |

**المجموع: ~13,700 سطر.** حجم كبير جداً مقارنة بالباك اند، ولم يخضع لنفس الانضباط المعماري.

### 🔴 أخطاء مؤكدة وتم إصلاحها بهالجلسة (موثّقة بالتفصيل بالتاريخ الكامل للمحادثة، ملخّصة هنا)

1. **تسريب صور منتجات مجاورة (Batch leakage):** بـ`page_bridge.js`، دالة `collectImagesNearMarkers` عندها منطق اختيار "أفضل مرشح" (Best Candidate) من عدة حاويات محتملة بشجرة React. نسخة معدَّلة من موديل آخر أضافت شرط عتبة (`images.length > 3`) بحالة الـ fallback (لما `visibleMatches=0` لكل المرشحين) — هذا الشرط كان يستبعد المرشح الصحيح لو معرضه لسا ما اكتمل تحميله، فيقفز لحاوية أوسع (Feed Array) فيها منتجات مجاورة. **الإصلاح:** رجّعت المنطق لسلوك النسخة القديمة المُثبتة (الأضيق بين كل المرشحين، بدون أي شرط عتبة) بنفس حالة الـ fallback فقط.
2. **رسوم خاطئة (250 مقابل 600 يوان):** الإكستنشن عندها قيمتين إعداد (`AddedFeeYuan`=250 للأحذية، `WatchFlatFeeYuan`=600 للساعات)، لكن **3 أماكن منفصلة** بـ`content.js` كانت تستخدم `AddedFeeYuan` دايماً بغض النظر عن نوع المنتج الفعلي (معاينة النافذة الفردية سطر ~1670، دالة `submitProduct` سطر ~2390، دالة `prepareBatchDraftForStore` سطر ~3654). **أُصلح موضعي الرفع الفعلي (2390 و3654)**؛ **موضع المعاينة (1670) لسا معطوب تجميلياً** (يعرض سعر خاطئ بالمعاينة قبل التأكيد، لكن السعر المُرسَل فعلياً صار صحيح).
3. **المقاسات ما تنضاف / تظهر كـ"Default" بسعر أعلى:** الباك اند يقرأ `ProductType` من **جوا كائن `Settings`** حصراً (`settings = data.get("Settings") if isinstance(dict) else data`)، ويتجاهل تماماً أي `ProductType` بالمستوى الأعلى للـ payload. الإكستنشن كانت تبني `Settings` بنشر `extractorConfig` كامل (`{...extractorConfig, ...}`) — وهذا يحمل `ProductType` **كإعداد عام ثابت محفوظ**، مو محسوب لكل منتج. **إصلاحي الأول (إضافة `ProductType` بالمستوى الأعلى فقط) كان بلا أي تأثير فعلي** لأنه بمكان ما يُقرأ منه إطلاقاً. **الإصلاح الصحيح:** فرض `ProductType: draft.productType` (أو `productType` بالمسار الفردي) **جوا كائن `Settings` نفسه**، بالمكانين (دفعة + فردي). **سبب الظهور بالأساس:** إعداد `ProductType` العام كان عالقاً على "watches" من جلسة سابقة (تأكد `detectProductType()` نفسها ترجع "shoes" صح للنص الفعلي — المشكلة كانت بالتوصيل، مو بالكشف).
4. **تكرار إرسال نفس المنتج للمتجر 5 مرات (نفس الـID):** لما الباك اند يكتشف تكراراً (409، نفس `SearchCode`/`StyleCode` — شائع لما المورد يعرض عدة صور/ألوان لنفس رقم الصنف كبطاقات منفصلة)، الإكستنشن كانت تسحب بيانات المنتج الموجود وتعيد إرسالها للمتجر **بدون توقف** (تعتبره "نجاح"). **الإصلاح:** إضافة `Set` مشترك (`seenProductIdsInBatch`) يتتبع أي `id` اتشاف بنفس الدفعة، ولو تكرر يُعرض بحالة بصرية مميزة ("مكرر ضمن الدفعة - تم تجاهله"، لون أصفر) بدل إعادة الإرسال. **ملاحظة:** الحماية الفعلية من تكرار حقيقي بمتجر Sooqify نفسه موجودة بطبقة ثانية (تأكد المستخدم إن المنتج المكرر فعلياً ما تكرر بالمتجر رغم المحاولات المتعددة) — لكن الإصلاح هذا يمنع الهدر/المحاولات غير الضرورية بغض النظر.
5. **خطأ `NameError` مؤكد بـ`/api/ai/extract-watch-variants`:** الـ route كان يستدعي `watch_variant_extractor.func(...)` بأسلوب `module.name` بدون وجود `import watch_variant_extractor` إطلاقاً بالملف (الدوال كانت مستوردة مباشرة بنفس الاسم). **أُصلح** بحذف بادئة الموديول (الدوال موجودة أصلاً بنفس الاسم).

### 🟡 نتائج جرد أولي — لسا ما أُصلحت (اكتشاف فقط)

1. **ميزة "حظر صورة" نصف موصولة:** `banImageUrl()` تشتغل فعلاً (زر 🚫 بواجهة المراجعة، تحفظ توقيع pHash للصورة). لكن `filterBannedImages()` — المفروض تُطبّق قائمة الحظر فعلياً على أي استخراج جديد — **غير مستدعاة من أي مكان بكامل المشروع**. المستخدم يحس إن الحظر اشتغل (يُحفظ فعلاً)، لكن ما له أي أثر عملي إطلاقاً.
2. **تعليق مضلّل بـ`content.js` (قرب سطر 1228):** يقول "تم تعطيل حد MaxImages وفلتر perceptual-hash عمداً" — صحيح جزئياً بس. الـ`MaxImages` cap فعلاً محذوف من منطق الدمج. لكن نظام الـpHash **مو معطّل بالكامل** — يشتغل فعلاً بميزة الحظر اليدوي (نقطة 1 فوق). التعليق يحتاج توضيح أدق.
3. **تكرار CSS حرفي:** كتلة كاملة (`.alphacode-btn-red/green/yellow` + حالات hover) مكررة **حرفياً مرتين** بـ`content.css` بمكانين مختلفين.
4. **نظامي إعدادات:** `content.js` عنده `extractorConfig`، و`admin_autofill.js` عنده `adminConfig` منفصل (مبني من `globalThis.ALPHACODE_DEFAULT_CONFIG || {}` — يشترك جزئياً مع نفس الافتراضيات عبر متغير عام، لكن كل ملف يحتفظ بنسخته المحلية القابلة للتغيير لحاله). يحتاج فحص تفصيلي: هل فيه تعارض قيم فعلي بينهم وقت التشغيل، أو فعلاً مسؤوليات منفصلة بحق.
5. **نمط تكرار بنيوي:** `submitProduct` (~141 سطر) و`prepareBatchDraftForStore` (~149 سطر) بـ`content.js` — نفس التدفق تقريباً (بناء payload → إرسال → معالجة 409 → استدعاء المتجر) بس بمسارين منفصلين كل وحد بمتغيراته الخاصة. **هذا هو السبب الجذري وراء تكرار نفس صنف الأخطاء (نقطة 2 و3 بالأخطاء المؤكدة) — أي إصلاح مستقبلي لازم يصير بمكان وحد لتفادي تكرار نفس الغلطة.**

### ما لم يُفحص إطلاقاً بعد
- `popup.js` (1447 سطر) — صفر فحص.
- `background.js` — فُحصت نقطة استقبال واحدة بس (`ENQUEUE_BATCH_PRODUCT`، سليمة، ما تكرر منطق فرض النوع/الرسوم). باقي الـ48 دالة لم تُفحص.
- `admin_autofill.js` — فُحصت دالة واحدة بس (`fillSizeVariants`، سليمة). باقي الـ50 دالة لم تُفحص.

---

## القسم 4: توصيتي المعمارية (للمناقشة، مو قرار نهائي)

**بخصوص "فصل منطق الساعات عن الأحذية" اللي طرحه المستخدم:** أوصي بالدمج لا الفصل. السبب المباشر: كل خطأ صلحناه بالقسم 3 (نقاط 2 و3) صار **بالضبط** لأن فيه مسارين منفصلين (فردي/دفعة) كل وحد يحسب نفس الشيء (الرسم، الإعدادات) بطريقته الخاصة — فلما تُصلح وحد، الثاني يبقى معطوب. الفصل الكامل لساعات/أحذية كنظامين منفصلين بيضاعف نفس هذا الخطر، مو يحلّه. البديل: نواة واحدة مشتركة + نقطة تفرّع واحدة واضحة حسب `productType` (نفس فلسفة `build_variant_fields` بالباك اند اللي تتفرّع داخلياً بدالة وحدة).

**بخصوص منهجية العمل على الإكستنشن:** أوصي بنفس منهجية الباك اند بالضبط — مرحلة جرد كاملة أولاً (إكمال ما بدأته: `popup.js`، باقي `background.js`/`admin_autofill.js`، فحص تفصيلي لتعارض الإعدادات) موثقة كملف مرجعي (زي `00_inventory.md`)، **قبل** أي تعديل UI أو منطق، مع قائمة قرارات صريحة تحتاج تأكيد المستخدم بدل الافتراض.

---

## القسم 5: أسئلة مفتوحة تحتاج قرار المستخدم (لا تفترض حلولاً لها)

1. **باك اند Phase 5:** هل التصحيح المُرسل لجيميناي نُفّذ فعلياً ونجح الاختبار؟ (راجع القسم 2).
2. **قيمة `WatchFlatFeeYuan`/`AddedFeeYuan`:** المستخدم صراحة قال إنه مو متأكد هل يفعّلهم بالباك اند أو يبقيهم بالإكستنشن فقط — هذا لسا قرار مفتوح.
3. **صيغة سعر الساعة (`build_watch_variations_from_absolute_yuan`):** موجودة بالباك اند لكن غير مستدعاة من أي route فعلياً — قرار 6 يقول "حافظ على سلوك extract الفعلي" بدون ربطها — هل الوقت مناسب الآن لربطها فعلياً؟
4. **ميزة حظر الصور اليدوي:** نصف موصولة — هل المطلوب توصيلها فعلياً (استدعاء `filterBannedImages`)، أو حذفها بالكامل بما إنها غير مستخدمة أصلاً؟
5. **فصل أو دمج منطق الساعات/الأحذية بالإكستنشن:** توصيتي بالقسم 4، لكن القرار النهائي للمستخدم.
